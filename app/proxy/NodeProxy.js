/**
 * @class App.proxy.NodeProxy
 * @extends Ext.data.proxy.Proxy
 * 
 * The custom proxy for fetching nodes
 */
Ext.define('App.proxy.NodeProxy', {
    extend: 'Ext.data.proxy.Proxy',
    alias: 'proxy.nodeproxy',
    
    config: {
        
    }
});